SQLITE=sqlite
CURL=curl
USERNAME=user
PASSWORD=pass
REPOS=https://omi.wekan.fi

# Network and HTTP Configuration
HTTP_METHOD=internal
HTTP_TIMEOUT=30
PREFER_INTERNAL_FEATURES=1

# Internal HTTP (for languages with built-in support)
# Set to 1 to use urllib (Python), HttpClient (C#), curl via /dev/tcp (Bash), etc
# Set to 0 to always use external curl/wget executables
USE_INTERNAL_HTTP=1

# Account Lockout Policy
ACCOUNTS_LOCKOUT_KNOWN_USERS_FAILURES_BEFORE=3
ACCOUNTS_LOCKOUT_KNOWN_USERS_PERIOD=60
ACCOUNTS_LOCKOUT_KNOWN_USERS_FAILURE_WINDOW=15
ACCOUNTS_LOCKOUT_UNKNOWN_USERS_FAILURES_BEFORE=3
ACCOUNTS_LOCKOUT_UNKNOWN_USERS_LOCKOUT_PERIOD=60
ACCOUNTS_LOCKOUT_UNKNOWN_USERS_FAILURE_WINDOW=15

# API Configuration
API_ENABLED=1
API_RATE_LIMIT=60
API_RATE_LIMIT_WINDOW=60
